package com.example.designpatterns.singletome.step3.inspring;

public class SingleTone {

    //Runtime 은 싱글톤을 사용하여 만들어짐.(getRuntime())을 통해서 실행할 수 있음.(new 가 되지 않음)
    Runtime runtime = Runtime.getRuntime();
}
